logging:
  pattern:
    file: '%d{yyyy-MM-dd HH:mm:ss} [%p] [%c{1}] - %m %n'
    console: '%d{yyyy-MM-dd HH:mm:ss} [%p] [%c{1}] - %m %n'
  file:
    name: application.log

spring:
  application:
    name: biblioteca